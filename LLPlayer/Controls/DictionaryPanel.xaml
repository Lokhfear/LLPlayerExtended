<UserControl x:Class="LLPlayer.Controls.DictionaryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:LLPlayer.ViewModels"
             xmlns:local="clr-namespace:LLPlayer.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DictionaryViewModel}"
             d:Foreground="White"
             d:Background="#1E1E1E"
             d:DesignHeight="450" d:DesignWidth="350"
             MinWidth="280" MinHeight="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <!-- null → Collapsed, иначе → Visible -->
        <local:NullToVisibilityConverter x:Key="NullToCollapsedConv" NullValue="Collapsed"/>
        <!-- null → Visible, иначе → Collapsed -->
        <local:NullToVisibilityConverter x:Key="NullToVisibleConv" NullValue="Visible"/>
        <!-- 0 → Visible, иначе → Collapsed -->
        <local:ZeroToVisibilityConverter x:Key="ZeroToVis"/>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Поиск + Refresh -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Search..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     FontSize="13"
                     Margin="0,0,4,0"/>
            
            <Button Grid.Column="1"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    ToolTip="Refresh"
                    Width="36" Height="36">
                <materialDesign:PackIcon Kind="Refresh" Width="18" Height="18"/>
            </Button>
        </Grid>

        <!-- Статистика -->
        <TextBlock Grid.Row="1"
                   Margin="0,0,0,8"
                   Opacity="0.5"
                   FontSize="11">
            <Run Text="{Binding TotalCount}"/> words
        </TextBlock>

        <!-- Список -->
        <Grid Grid.Row="2">
            <ProgressBar IsIndeterminate="True"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                         VerticalAlignment="Top" Height="2"/>
            
            <TextBlock Text="No words yet"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Opacity="0.4"
                       Visibility="{Binding Entries.Count, Converter={StaticResource ZeroToVis}}"/>
            
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Entries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#333" BorderThickness="0,0,0,1" Padding="4,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Word}" FontWeight="SemiBold" FontSize="14"/>
                                            <TextBlock Text=" — " Opacity="0.4" 
                                                       Visibility="{Binding Translation, Converter={StaticResource NullToCollapsedConv}}"/>
                                            <TextBlock Text="{Binding Translation}" FontSize="14" 
                                                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                       Visibility="{Binding Translation, Converter={StaticResource NullToCollapsedConv}}"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Sentence}" FontStyle="Italic" 
                                                   Opacity="0.6" FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    
                                    <Button Grid.Column="1" VerticalAlignment="Top"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignToolButton}"
                                            ToolTip="Delete">
                                        <materialDesign:PackIcon Kind="Delete" Width="14" Height="14" Opacity="0.5"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
