<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- ============================================ -->
    <!-- MODERN DESIGN SYSTEM FOR LLPLAYER            -->
    <!-- A clean, mobile-inspired visual language     -->
    <!-- ============================================ -->

    <!-- ============================= -->
    <!-- COLOR PALETTE                 -->
    <!-- ============================= -->
    
    <!-- Primary brand color (kept from original) -->
    <Color x:Key="PrimaryColor">#D23D6F</Color>
    <Color x:Key="PrimaryLightColor">#FF759D</Color>
    <Color x:Key="PrimaryDarkColor">#9A1C4A</Color>
    
    <!-- Secondary accent color -->
    <Color x:Key="SecondaryColor">#00B8D4</Color>
    <Color x:Key="SecondaryLightColor">#5CEAFF</Color>
    <Color x:Key="SecondaryDarkColor">#008BA3</Color>
    
    <!-- Surface colors - lighter and cleaner -->
    <Color x:Key="SurfaceColor">#1E1E2E</Color>
    <Color x:Key="SurfaceLightColor">#2A2A3E</Color>
    <Color x:Key="SurfaceBrightColor">#35354F</Color>
    
    <!-- Background colors -->
    <Color x:Key="BackgroundColor">#12121C</Color>
    <Color x:Key="BackgroundLightColor">#1A1A28</Color>
    
    <!-- Text colors -->
    <Color x:Key="TextPrimaryColor">#FFFFFF</Color>
    <Color x:Key="TextSecondaryColor">#B0B0C0</Color>
    <Color x:Key="TextTertiaryColor">#787890</Color>
    <Color x:Key="TextDisabledColor">#505060</Color>
    
    <!-- Status colors -->
    <Color x:Key="SuccessColor">#4CAF50</Color>
    <Color x:Key="WarningColor">#FF9800</Color>
    <Color x:Key="ErrorColor">#F44336</Color>
    <Color x:Key="InfoColor">#2196F3</Color>
    
    <!-- Border colors -->
    <Color x:Key="BorderColor">#2A2A3E</Color>
    <Color x:Key="BorderLightColor">#3A3A4E</Color>
    
    <!-- Overlay colors -->
    <Color x:Key="OverlayColor">#000000</Color>
    <Color x:Key="ScrimColor">#000000</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
    <SolidColorBrush x:Key="SecondaryLightBrush" Color="{StaticResource SecondaryLightColor}"/>
    <SolidColorBrush x:Key="SecondaryDarkBrush" Color="{StaticResource SecondaryDarkColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceLightBrush" Color="{StaticResource SurfaceLightColor}"/>
    <SolidColorBrush x:Key="SurfaceBrightBrush" Color="{StaticResource SurfaceBrightColor}"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="BackgroundLightBrush" Color="{StaticResource BackgroundLightColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextTertiaryBrush" Color="{StaticResource TextTertiaryColor}"/>
    <SolidColorBrush x:Key="TextDisabledBrush" Color="{StaticResource TextDisabledColor}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="BorderLightBrush" Color="{StaticResource BorderLightColor}"/>

    <!-- ============================= -->
    <!-- TYPOGRAPHY SCALE              -->
    <!-- ============================= -->
    
    <!-- Font Family -->
    <FontFamily x:Key="PrimaryFont">Segoe UI Variable, Segoe UI, Roboto, Helvetica Neue, Arial, sans-serif</FontFamily>
    <FontFamily x:Key="DisplayFont">Segoe UI Variable Display, Segoe UI Light, sans-serif</FontFamily>
    <FontFamily x:Key="MonoFont">Cascadia Code, Consolas, monospace</FontFamily>
    
    <!-- Font Sizes - Mobile-inspired scale -->
    <sys:Double x:Key="FontSizeXxs">10</sys:Double>
    <sys:Double x:Key="FontSizeXs">11</sys:Double>
    <sys:Double x:Key="FontSizeSm">12</sys:Double>
    <sys:Double x:Key="FontSizeMd">14</sys:Double>
    <sys:Double x:Key="FontSizeLg">16</sys:Double>
    <sys:Double x:Key="FontSizeXl">18</sys:Double>
    <sys:Double x:Key="FontSizeXxl">20</sys:Double>
    <sys:Double x:Key="FontSizeDisplay1">24</sys:Double>
    <sys:Double x:Key="FontSizeDisplay2">28</sys:Double>
    <sys:Double x:Key="FontSizeDisplay3">34</sys:Double>
    
    <!-- Font Weights -->
    <FontWeight x:Key="FontWeightLight">Light</FontWeight>
    <FontWeight x:Key="FontWeightRegular">Regular</FontWeight>
    <FontWeight x:Key="FontWeightMedium">Medium</FontWeight>
    <FontWeight x:Key="FontWeightSemiBold">SemiBold</FontWeight>
    <FontWeight x:Key="FontWeightBold">Bold</FontWeight>

    <!-- Typography Styles -->
    <Style x:Key="TextCaption" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXs}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightRegular}"/>
        <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
        <Setter Property="LineHeight" Value="16"/>
    </Style>
    
    <Style x:Key="TextOverline" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXxs}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightMedium}"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="CharacterSpacing" Value="50"/>
    </Style>
    
    <Style x:Key="TextBody2" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeSm}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightRegular}"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="LineHeight" Value="20"/>
    </Style>
    
    <Style x:Key="TextBody1" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeMd}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightRegular}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="LineHeight" Value="22"/>
    </Style>
    
    <Style x:Key="TextSubtitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeLg}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightMedium}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="LineHeight" Value="24"/>
    </Style>
    
    <Style x:Key="TextTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeXxl}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightSemiBold}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="LineHeight" Value="28"/>
    </Style>
    
    <Style x:Key="TextHeadline" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource FontSizeDisplay1}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightLight}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="LineHeight" Value="32"/>
    </Style>

    <!-- ============================= -->
    <!-- SPACING SYSTEM                -->
    <!-- ============================= -->
    
    <!-- Base unit: 4px -->
    <CornerRadius x:Key="CornerNone">0</CornerRadius>
    <CornerRadius x:Key="CornerXs">2</CornerRadius>
    <CornerRadius x:Key="CornerSm">4</CornerRadius>
    <CornerRadius x:Key="CornerMd">8</CornerRadius>
    <CornerRadius x:Key="CornerLg">12</CornerRadius>
    <CornerRadius x:Key="CornerXl">16</CornerRadius>
    <CornerRadius x:Key="CornerXxl">24</CornerRadius>
    <CornerRadius x:Key="CornerFull">9999</CornerRadius>

    <!-- ============================= -->
    <!-- ELEVATION & SHADOWS           -->
    <!-- ============================= -->
    
    <!-- Subtle shadows for modern flat look -->
    <DropShadowEffect x:Key="Shadow1" BlurRadius="8" ShadowDepth="1" Opacity="0.15" Direction="270"/>
    <DropShadowEffect x:Key="Shadow2" BlurRadius="12" ShadowDepth="2" Opacity="0.2" Direction="270"/>
    <DropShadowEffect x:Key="Shadow3" BlurRadius="16" ShadowDepth="4" Opacity="0.25" Direction="270"/>
    <DropShadowEffect x:Key="Shadow4" BlurRadius="24" ShadowDepth="8" Opacity="0.3" Direction="270"/>

    <!-- ============================= -->
    <!-- BUTTON STYLES                 -->
    <!-- ============================= -->
    
    <!-- Base Button Style - Modern, minimal -->
    <Style x:Key="ModernButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Height" Value="40"/>
        <Setter Property="Padding" Value="20,0"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeMd}"/>
        <Setter Property="FontWeight" Value="{StaticResource FontWeightMedium}"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerMd}"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="{TemplateBinding materialDesign:ButtonAssist.CornerRadius, FallbackValue={StaticResource CornerMd}}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Outlined Button -->
    <Style x:Key="ModernOutlinedButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
    </Style>
    
    <!-- Text Button -->
    <Style x:Key="ModernTextButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,0"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
    </Style>
    
    <!-- Icon Button - Smaller, cleaner -->
    <Style x:Key="ModernIconButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
        <Setter Property="Width" Value="36"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="{StaticResource CornerFull}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#1AFFFFFF"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#2AFFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Small Icon Button -->
    <Style x:Key="ModernIconButtonSmall" TargetType="Button" BasedOn="{StaticResource ModernIconButton}">
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="28"/>
    </Style>

    <!-- ============================= -->
    <!-- CARD STYLES                   -->
    <!-- ============================= -->
    
    <!-- Modern Card - Minimal, subtle borders -->
    <Style x:Key="ModernCard" TargetType="materialDesign:Card">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerMd}"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="0,4"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>
    
    <!-- Interactive Card -->
    <Style x:Key="ModernInteractiveCard" TargetType="materialDesign:Card" BasedOn="{StaticResource ModernCard}">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="materialDesign:Card">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding materialDesign:CardAssist.CornerRadius, FallbackValue={StaticResource CornerMd}}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================= -->
    <!-- INPUT STYLES                  -->
    <!-- ============================= -->
    
    <!-- Modern TextBox -->
    <Style x:Key="ModernTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeMd}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="materialDesign:HintAssist.HintOpacity" Value="0.6"/>
        <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="materialDesign:TextFieldAssist.TextBoxViewMargin" Value="4"/>
    </Style>
    
    <!-- Modern Outlined TextBox -->
    <Style x:Key="ModernOutlinedTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeMd}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="materialDesign:HintAssist.HintOpacity" Value="0.6"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
        <Setter Property="materialDesign:TextFieldAssist.TextBoxViewMargin" Value="4"/>
    </Style>

    <!-- ============================= -->
    <!-- LIST ITEM STYLES              -->
    <!-- ============================= -->
    
    <!-- Modern ListBox Item -->
    <Style x:Key="ModernListBoxItem" TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Margin" Value="0,2"/>
        <Setter Property="materialDesign:RippleAssist.IsDisabled" Value="True"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource CornerSm}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#0AFFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#1AFFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================= -->
    <!-- BADGE / CHIP STYLES           -->
    <!-- ============================= -->
    
    <!-- Status Badge -->
    <Style x:Key="StatusBadge" TargetType="Border">
        <Setter Property="CornerRadius" Value="{StaticResource CornerFull}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

    <!-- ============================= -->
    <!-- DIVIDER                       -->
    <!-- ============================= -->
    
    <Style x:Key="ModernDivider" TargetType="Separator">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Background" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Margin" Value="0,8"/>
    </Style>

    <!-- ============================= -->
    <!-- TOOLBAR                       -->
    <!-- ============================= -->
    
    <Style x:Key="ModernToolBar" TargetType="ToolBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

</ResourceDictionary>
